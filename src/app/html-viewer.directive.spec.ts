import { HtmlViewerDirective } from './html-viewer.directive';

describe('HtmlViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new HtmlViewerDirective();
    expect(directive).toBeTruthy();
  });
});
